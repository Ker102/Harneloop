# Core MVP Implementation Plan

> **For Agent:** Use executing-plans skill to implement this plan task-by-task.

**Goal:** Build the first task-family-neutral EvoRig lifecycle engine.

**Architecture:** A Python CLI manipulates portable harness-unit directories. The engine stores human-readable YAML and Markdown control files, creates candidate patch workspaces, validates protected boundaries, promotes candidates into restorable snapshots, supports rollback, creates thin packages, and records wait/stop/resume state.

**Tech Stack:** Python 3.11+, PyYAML, standard-library `argparse`, `tarfile`, `hashlib`, and `unittest`.

---

## Task 1: Project Scaffold

**Files:**
- Create: `pyproject.toml`
- Create: `src/evorig/__init__.py`
- Create: `src/evorig/__main__.py`
- Create: `src/evorig/cli.py`
- Create: `tests/test_core_lifecycle.py`

**Steps:**

1. Add a package with a runnable `python -m evorig` entrypoint.
2. Add a smoke test that imports `evorig`.
3. Run `python -m unittest discover -s tests`.

## Task 2: Unit Creation

**Files:**
- Create: `src/evorig/unit.py`
- Create: `src/evorig/state.py`
- Create: `src/evorig/yamlio.py`
- Test: `tests/test_core_lifecycle.py`

**Steps:**

1. Write a failing test for `init_unit`.
2. Implement unit directory creation with `unit.yaml`, `UNIT_AGENT.md`, `.evolve/state.json`, `CURRENT_STATE.md`, and `NEXT_ACTION.md`.
3. Add the `init-unit` CLI command.
4. Run `python -m unittest discover -s tests`.

## Task 3: Candidate Creation

**Files:**
- Create: `src/evorig/candidate.py`
- Test: `tests/test_core_lifecycle.py`

**Steps:**

1. Write a failing test for creating `cand-0001`.
2. Implement candidate metadata and workspace folders.
3. Update framework state with the active candidate.
4. Add the `candidate create` CLI command.
5. Run `python -m unittest discover -s tests`.

## Task 4: Protected Promotion And Snapshots

**Files:**
- Create: `src/evorig/versioning.py`
- Create: `src/evorig/paths.py`
- Create: `src/evorig/errors.py`
- Test: `tests/test_core_lifecycle.py`

**Steps:**

1. Write a failing test that a candidate overlay can promote a normal file.
2. Write a failing test that a candidate overlay cannot edit `unit.yaml`.
3. Implement overlay application, protected-path checks, manifests, hashing, and snapshot creation.
4. Add the `promote` CLI command.
5. Run `python -m unittest discover -s tests`.

## Task 5: Rollback

**Files:**
- Modify: `src/evorig/versioning.py`
- Test: `tests/test_core_lifecycle.py`

**Steps:**

1. Write a failing test that rollback restores an older file version.
2. Implement rollback from `versions/<version>/snapshot`.
3. Add the `rollback` CLI command.
4. Run `python -m unittest discover -s tests`.

## Task 6: Thin Packaging

**Files:**
- Create: `src/evorig/packaging.py`
- Test: `tests/test_core_lifecycle.py`

**Steps:**

1. Write a failing test that a promoted unit can be packaged.
2. Implement `tar.gz` packaging from a promoted snapshot.
3. Exclude runtime state, candidates, raw artifacts, caches, and secrets.
4. Add the `package` CLI command.
5. Run `python -m unittest discover -s tests`.

## Task 7: Wait, Stop, And Status

**Files:**
- Modify: `src/evorig/state.py`
- Modify: `src/evorig/cli.py`
- Test: `tests/test_core_lifecycle.py`

**Steps:**

1. Write a failing test for waiting state.
2. Write a failing test for stopped state.
3. Implement state transitions that update `.evolve/state.json`, `CURRENT_STATE.md`, and `NEXT_ACTION.md`.
4. Add `state wait`, `state stop`, `state resume`, and `status` CLI commands.
5. Run `python -m unittest discover -s tests`.
